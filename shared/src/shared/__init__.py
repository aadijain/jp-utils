"""jp-utils shared contract models.

Plain stdlib dataclasses shared between the backend (imported normally) and the
Anki add-on (vendored into the .ankiaddon zip at build time). Stdlib-only: no
third-party imports may appear in this package.
"""
